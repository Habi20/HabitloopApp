import type { User as SchemaUser } from "../../../shared/schema";

export interface User extends SchemaUser {
  // Add any client-specific fields or overrides here
  // Example:
  // fullName?: string;
}
